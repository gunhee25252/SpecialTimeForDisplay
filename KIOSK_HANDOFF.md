# 특별시 키오스크 실행 안내

## 세팅 체크리스트

| 항목 | 내용 |
| --- | --- |
| 폴더 배치 | `C:\SpecialTimeKiosk`처럼 쓰기 가능한 폴더에 `kiosk-package` 전체를 복사 |
| 기본 프린터 | 사진 프린터를 기본 프린터로 지정, 용지 4×6인치·여백 없음으로 기본 설정 변경 |
| 카메라 | 비율은 기본값(16:9) 유지, 브라우저에서 카메라 권한 미리 허용 |
| 자동 실행 | 로그온 시 `SpecialTimeKiosk.exe` 자동 실행 등록 |
| 디스플레이 | 세로 방향, 배율 100% |
| 전원 관리 | 화면 끄기·절전·화면 보호기 모두 해제 |

각 항목의 자세한 내용은 아래에 있습니다.

## 실행

`SpecialTimeKiosk.exe`를 더블클릭하면 터미널 없이 로컬 서버가 실행되고 기본 브라우저가 열립니다.

- 접속 주소: `http://127.0.0.1:4173`
- 인터넷 연결: 필요 없음
- 결과물 저장 위치: `print-results` 폴더

실행파일이 켜져 있는 동안 브라우저를 닫아도 같은 주소로 다시 접속할 수 있습니다. 서버를 종료하려면 Windows 작업 관리자에서 `SpecialTimeKiosk`를 종료하거나 PC를 재부팅합니다.

## 키오스크 업체 설정

Windows 로그인 시 `SpecialTimeKiosk.exe`가 먼저 자동 실행되도록 등록합니다. 업체에서 Chrome 또는 Edge를 별도로 키오스크 모드로 실행한다면 `kiosk-settings.json`의 `openBrowser`를 `false`로 바꿉니다.

설정 파일을 바꾸지 않고 서버만 실행하려면 시작 프로그램의 실행 인수에 `--server-only`를 추가해도 됩니다.

브라우저를 별도로 실행할 때는 서버 실행 후 약 2~3초 뒤에 브라우저가 열리도록 순서를 설정합니다. 실행파일은 결과 저장 권한이 필요한 `Program Files`보다 `C:\SpecialTimeKiosk`처럼 쓰기 가능한 폴더에 배치합니다.

브라우저는 다음 주소를 전체화면으로 엽니다.

```text
http://127.0.0.1:4173
```

## 인쇄 방식 (중요)

인쇄는 **브라우저가 아니라 실행파일이 직접** 처리합니다. `인쇄하기`를 누르면 완성된 이미지가 실행파일로 전송되고, 실행파일이 `print-results`에 저장한 뒤 곧바로 프린터로 보냅니다. 브라우저는 인쇄에 관여하지 않으므로 **인쇄 대화상자가 나타나지 않습니다.**

관련 설정은 `kiosk-settings.json`에 있습니다.

| 항목 | 설명 |
| --- | --- |
| `silentPrint` | `true`면 실행파일이 직접 인쇄합니다. `false`로 두면 브라우저가 인쇄하며 이때는 대화상자가 나타납니다. |
| `printerName` | 사용할 프린터 이름. 비워 두면 Windows 기본 프린터를 씁니다. |
| `paperSizeName` | 용지 이름을 강제로 지정할 때만 입력합니다. 비워 두면 4×6인치에 가장 가까운 용지를 자동으로 고릅니다. |
| `printToFilePath` | 확인용. 경로를 넣으면 용지 대신 그 파일로 출력합니다. 운영 시에는 반드시 비워 두십시오. |

인쇄 결과와 실패 사유는 실행파일과 같은 폴더의 `kiosk-server.log`에 남습니다.

```text
[2026-08-17 17:04:12] 인쇄 전송: print-901.png → Microsoft Print to PDF (용지 North America 4x 6 400x600)
```

### 종이를 쓰지 않고 확인하기

`printerName`을 `Microsoft Print to PDF`로, `printToFilePath`를 `C:\SpecialTimeKiosk\test.pdf`처럼 지정하고 한 번 인쇄해 보십시오. 대화상자 없이 PDF가 만들어지면 정상입니다. 확인 후 두 값을 다시 비워 둡니다.

## 브라우저 전체화면

`kiosk-settings.json`의 `browserPath`에 Edge 또는 Chrome의 전체 경로를 넣으면, 실행파일이 그 브라우저를 `browserArguments`와 함께 직접 실행합니다. `--kiosk`가 전체화면을 담당합니다. 인쇄는 위에서 설명한 대로 실행파일이 처리하므로 브라우저 인쇄 옵션은 필요하지 않습니다.

```json
{
  "browserPath": "C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe",
  "browserArguments": "--kiosk --no-first-run --no-default-browser-check"
}
```

Chrome을 쓴다면 `C:\Program Files\Google\Chrome\Application\chrome.exe`를 넣습니다. `browserPath`를 비워 두면 지금까지처럼 기본 브라우저가 옵션 없이 열리고, 이 경우에는 인쇄 대화상자가 나타납니다.

### 전용 브라우저 프로필

실행파일은 `--user-data-dir` 인수를 자동으로 붙여 실행파일과 같은 폴더의 `browser-profile`을 사용합니다.

이미 실행 중인 Edge나 Chrome이 있으면, 새 브라우저가 뜨지 않고 **기존 창에 주소만 전달되면서 실행 인수가 전부 무시됩니다.** 그러면 전체화면도 되지 않고 인쇄 대화상자도 그대로 나타납니다. 전용 프로필을 쓰면 항상 별개의 브라우저로 실행되어 인수가 정상 적용됩니다.

`browserArguments`에 `--user-data-dir`을 직접 적으면 그 값이 우선합니다. 이 폴더를 지우면 카메라 권한 허용 기록도 함께 사라지므로 지우지 마십시오.

### 전체화면이 되지 않을 때

브라우저 버전에 따라 `--kiosk`만으로 전체화면이 되지 않으면 Edge 전용 인수를 함께 씁니다.

```text
--kiosk --edge-kiosk-type=fullscreen --kiosk-idle-timeout-minutes=0 --no-first-run
```

`--kiosk-idle-timeout-minutes=0`은 Edge가 일정 시간 뒤 세션을 초기화하는 동작을 끕니다. 이 값을 빼면 인쇄 번호와 카메라 권한이 초기화될 수 있습니다.

전체화면을 종료할 때는 `Alt`+`F4`를 사용합니다.

## 카메라 설정

메인 화면 오른쪽 아래 카메라 아이콘으로 들어가는 `한 컷 사진관`에서 웹캠을 사용합니다. 브라우저가 카메라 권한을 한 번 물어보면 허용으로 저장합니다.

카메라 유틸리티나 드라이버에서 **화면 비율을 바꾸지 마시고 기본값(16:9)으로 두십시오.** 인화지에 맞는 세로 2:3 잘라내기는 프로그램이 직접 처리합니다. 카메라 쪽에서 1:1 같은 비율로 바꾸면 제품에 따라 화면을 잘라내지 않고 눌러서 맞추기 때문에 얼굴이 세로로 길어진 사진이 나옵니다.

해상도는 높을수록 좋습니다(1920×1080 권장). 세로 해상도가 인화 화질을 결정합니다.

### 카메라 권한 미리 허용

권한은 실행파일이 사용하는 전용 프로필(`browser-profile`)에 저장되므로, **키오스크로 실행된 그 브라우저 안에서** 한 번 허용해야 합니다. 평소 쓰는 Edge 창에서 허용한 것은 넘어오지 않습니다.

1. `SpecialTimeKiosk.exe`를 실행합니다. Edge가 전체화면으로 열립니다.
2. 메인 화면 오른쪽 아래 카메라 아이콘을 눌러 `한 컷 사진관`에 들어갑니다.
3. 카메라 권한 요청이 뜨면 **허용**을 누릅니다.
4. 확인 후 `Alt`+`F4`로 닫습니다.

한 번 허용하면 `browser-profile` 폴더에 저장되어 이후에는 다시 묻지 않습니다.

## 프린터 준비

실행파일이 용지 크기를 4×6인치로 지정해 인쇄하지만, 용지 종류와 여백 없음(Borderless) 설정은 프린터 쪽에 따릅니다. 아래를 미리 마쳐 두십시오.

1. 설정 → 블루투스 및 장치 → 프린터 및 스캐너에서 **"Windows에서 기본 프린터 관리"를 끄고** 사진 프린터를 기본 프린터로 지정합니다. (또는 `kiosk-settings.json`의 `printerName`에 프린터 이름을 정확히 입력합니다.)
2. 해당 프린터의 **인쇄 기본 설정**에서 용지 크기 `4×6인치(10×15cm)`, 여백 `없음(Borderless)`, 방향 `세로`, 용지 종류 `사진 용지`로 지정합니다.
3. 시험 인화를 한 장 뽑아 잘림이나 여백이 없는지 확인한 뒤 고정합니다.

프린터 속성 창에서 일시적으로 바꾼 값은 적용되지 않을 수 있으므로, 반드시 프린터의 기본 설정 자체를 변경해야 합니다.

인쇄가 되지 않으면 `kiosk-server.log`에 `인쇄 실패:`로 시작하는 줄이 남습니다.

## 자동 실행

로그온 시 `SpecialTimeKiosk.exe`가 실행되도록 등록합니다. 실행 파일의 바로가기를 `shell:startup` 폴더에 넣거나, 작업 스케줄러에서 로그온 트리거로 등록합니다.

`kiosk-settings.json`에 `browserPath`를 지정해 두었다면 브라우저는 실행파일이 알아서 띄우므로 따로 등록할 필요가 없습니다.

## 디스플레이와 전원 관리

화면 기준 해상도는 세로 1080×1920입니다. 디스플레이가 세로형이면 Windows 디스플레이 설정에서 방향을 `세로`로, 배율을 `100%`로 맞춥니다. 화면 비율이 달라도 비율을 유지한 채 축소되어 표시되며 화면이 찌그러지지는 않습니다.

행사 도중 화면이 꺼지지 않도록 전원 옵션에서 다음을 모두 해제합니다.

- 화면 끄기: 안 함
- 절전 모드로 전환: 안 함
- 화면 보호기: 없음

가능하면 Windows Update가 행사 시간에 자동 재시작하지 않도록 활성 시간을 지정합니다.

## 결과물 저장

`인쇄하기`를 누를 때마다 다음 두 파일이 `print-results`에 저장됩니다.

```text
print-001.png
print-001.json
```

사진 저장은 실제 인쇄와 별도로 동작하므로, 인쇄를 취소해도 이미 생성된 결과 파일은 남습니다. 반대로 저장에 실패해도 인쇄는 그대로 진행됩니다.

## 그 밖에

- 5분 동안 아무 입력이 없으면 자동으로 처음 화면으로 돌아갑니다.
- 문제가 생기면 실행파일과 같은 폴더의 `kiosk-server.log`를 확인합니다.

## 폴더 주의사항

실행파일만 따로 옮기지 말고 `kiosk-package` 폴더 전체를 그대로 복사합니다. 특히 `web`, `print-results`, `kiosk-settings.json`이 실행파일과 같은 위치에 있어야 합니다.

이 실행파일은 Windows x64용이며 코드 서명 인증서를 적용하지 않았습니다. 처음 실행할 때 Windows SmartScreen 경고가 나타나면 업체 세팅 과정에서 실행을 허용해야 합니다.
